#!/usr/bin/env bash
# This variable contains absolute path of this `event_manager` script
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null && pwd )"
java -jar $DIR/../target/event-manager.jar server $1